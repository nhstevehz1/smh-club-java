import { TestBed } from '@angular/core/testing';

import { EditDialogFactoryService } from './edit-dialog-factory.service';

/*describe('EditDialogFactoryService', () => {
  let service: EditDialogFactoryService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(EditDialogFactoryService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});*/
