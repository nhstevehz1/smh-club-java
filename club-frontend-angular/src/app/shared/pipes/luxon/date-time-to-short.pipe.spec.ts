import {DateTimeToShortPipe} from './date-time-to-short.pipe';

describe('DateTimeToShortPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeToShortPipe();
    expect(pipe).toBeTruthy();
  });
});
